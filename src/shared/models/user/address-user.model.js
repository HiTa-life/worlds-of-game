export class AddressUser {
    constructor (  address, 
        city, 
        zipCode)
        {
            this.address = address;
            this.city = city;
            this.zipCode = zipCode
        };
};